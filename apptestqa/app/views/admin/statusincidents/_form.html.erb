<%= form_for @statusincident, url: statusincident do |f| %>
  <% if @statusincident.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@statusincident.errors.count, "error") %> prohibited this admin_statusincident from being saved:</h2>

      <ul>
      <% @statusincident.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
