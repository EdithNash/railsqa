
<%= form_for @severity, url: severity do |f| %>
  <% if @severity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@severity.errors.count, "error") %> prohibited this admin_severity from being saved:</h2>

      <ul>
      <% @severity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
