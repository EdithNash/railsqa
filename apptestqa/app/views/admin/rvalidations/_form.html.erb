
<%= form_for @rvalidation, url: rvalidation do |f| %>
  <% if @rvalidation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rvalidation.errors.count, "error") %> prohibited this admin_rvalidation from being saved:</h2>

      <ul>
      <% @rvalidation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
